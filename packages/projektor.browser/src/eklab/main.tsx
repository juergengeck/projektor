import React from "react";
import ReactDOM from "react-dom/client";
import Lab from "./Lab";
import "../../../amway.app/ui/styles.css";

ReactDOM.createRoot(document.getElementById("eklab-root")!).render(
  <React.StrictMode>
    <Lab />
  </React.StrictMode>,
);
